{
  "name": "fusionmix-quantum",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "echo 'Use Firebase emulators instead'",
    "deploy": "npm run build && firebase deploy"
  },
  "dependencies": {
    "firebase": "^10.7.1"
  },
  "engines": {
    "node": ">=18"
  }
}
