{
  "name": "fusionmix-quantum",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "deploy": "npm run build && firebase deploy --only hosting"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18"
  }
}
